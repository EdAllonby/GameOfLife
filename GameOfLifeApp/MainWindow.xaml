<Window x:Class="GameOfLifeApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525"
        xmlns:cellGrid="clr-namespace:GameOfLifeApp">
   
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel  Grid.Row="0" Orientation="Horizontal">
            <Button HorizontalAlignment="Left" Height="24" Width="75" Content="Start" Margin="10" Click="ButtonBase_OnClick"/>
        </StackPanel>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Slider VerticalAlignment="Top" Grid.Column="0" Orientation="Vertical" Width="20" Height="200" Maximum="1000" Minimum="100" Margin="20" ValueChanged="OnSimulationSpeedChanged" />
            
            <cellGrid:CellGridUserControl x:Name="CellGrid" Grid.Column="1" />

        </Grid>
        
    </Grid>

</Window>
